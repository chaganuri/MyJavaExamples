package com.collections;

import java.util.Iterator;
import java.util.concurrent.CopyOnWriteArrayList;

public class ArrayDuplicatesTest {

	public static void main(String[] args) {
		
		/*int[] intArr = {1,2,2,3,5,6,9,6,5,9};
		int[] dummyArr = new int[intArr.length];
		for (int i = 0; i < intArr.length; i++) {
			
			dummyArr[intArr[i]] =1;
		}
		//Missing numbers in Array
		for (int i = 1; i < dummyArr.length; i++) {
			if(dummyArr[i]==0)
				System.out.println("ArrayDuplicatesTest.main()::"+i);
		}
		
*/		/*String str = null;
		String str1 = "cab";
		System.out.println(str1.equals("cab") | str.equals(null));*/
		
		/*CopyOnWriteArrayList<Integer> numbers = new CopyOnWriteArrayList<>(new Integer[]{1, 3, 5, 8});
		
		for (Integer integer : numbers) {
			System.out.println("ArrayDuplicatesTest.main():: "+integer);
		}
		Iterator<Integer> iterator = numbers.iterator();
		numbers.add(10);
		for (Integer integer : numbers) {
			System.out.println("ArrayDuplicatesTest.main()::After add "+integer);
		}
		
		while (iterator.hasNext()) {
			Integer integer = (Integer) iterator.next();
			System.out.println("ArrayDuplicatesTest.main()::using iterator after add "+integer);
			
		}
*/
		

	}
	
	
}
